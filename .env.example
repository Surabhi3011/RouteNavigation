# Placeholder for a future routing API key, in case the public OSRM demo
# server is swapped out for a paid/self-hosted routing provider later.
# Not currently read by the app — OSRM's public demo server needs no key.
ROUTING_API_KEY=
PORT=3000
